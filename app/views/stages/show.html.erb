<h1><%= @stage.title %></h1>
<p><%= @stage.description %></p>

<%= link_to 'Reset', reset_stage_path(@stage), method: :put %>

<h2> Performance: </h2>
<p><%= @stage.performance %></p>

<h2> Messages: </h2>
<%= render 'messages/messages', stage: @stage %>
